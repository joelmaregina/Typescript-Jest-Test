describe('TESTANDO UMA CONST', () => {
  it('Should return one', () => {
    const number = 6;
    expect(number).toBe(6);
  });
});

describe('TESTANDO UMA STRING', () => {
  test('Should return Joelma', () => {
    const name = 'Joelma';
    expect(name).toBe('Joelma');
  });
});
